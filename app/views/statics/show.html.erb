<div class="card mt-2">
  <div class="card-header">
    <h4>
      MMSI <%= @source.mmsi.to_s %>
    </h4>
  </div>
  <div class="card-body">
    <%= render 'shared/card',
      title: "Static data",
      rows: @message_rows,
      headers: @message_headers
    %>
    <%= render 'shared/card',
      title: "Dimensions",
      rows: @dimension_rows,
      headers: @dimension_headers
    %>
  </div>
</div>
