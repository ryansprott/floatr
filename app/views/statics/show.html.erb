<div class="card mt-2">
  <div class="card-header">
    <h4>
      MMSI <%= @source.mmsi.to_s %>
    </h4>
  </div>
  <div class="card-body">
    <%= render 'shared/card',
      title: "Static data",
      headers: @message_table.specific_headers,
      rows: @message_table.specific_rows
    %>
    <%= render 'shared/card',
      title: "Dimensions",
      rows: @message_table.dimension_rows,
      headers: @message_table.dimension_headers
    %>
  </div>
</div>
